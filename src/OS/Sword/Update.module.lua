--[[
OSes need a way to update to their latest release
Some users prefer to use older versions, so giving them the option to receive 
updates is needed.
]]

local System = {
	SystemFile = true,
	Required = true
}

local function Update()
	
end
